# petals

## 🐳 Quick Start with Docker

This repository includes a standard Docker configuration for easy development.

1. Ensure Docker Desktop is running.
2. Run `docker compose up --build` to start the environment.
3. The container will mount the current directory to `/app`.

> A brief, one-sentence elevator pitch describing the systems purpose.

## 🏗 Architecture

Provide a high-level overview of the system design.

* **Pattern:** e.g. Microservices, Monolithic, Event-driven
* **Primary Stack:** e.g. Python, Rust, PostgreSQL

## 🚀 Quick Start

### Prerequisites

* List version requirements e.g. Node.js >= 18.x

### 🖥️ Windows Users

> [!IMPORTANT]
> **WSL Necessary**: This project relies on `uvloop` (via `hivemind`), which is **not supported on native Windows**.
> For a plug-and-play experience, you **MUST** use [WSL (Windows Subsystem for Linux)](https://learn.microsoft.com/en-us/windows/wsl/install).

### Installation

```bash
git clone https://github.com/thookham/petals.git
cd petals
# pip install -r requirements.txt
# npm install
```

## 🛠 Usage

```python
# Example of the primary entry point
# import project
# project.initialize()
```

## 📄 License

Distributed under the [LICENSE TYPE] License. See LICENSE for more information.

---
